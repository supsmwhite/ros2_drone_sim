/**:
  ros__parameters:
    frame_id: map

    # [xmin, xmax, ymin, ymax, zmin, zmax]
    workspace: [-1.0, 14.5, -2.5, 7.0, -0.5, 5.0]

    # Each group is [center_x, center_y, center_z, size_x, size_y, size_z].
    # Three boundary-connected walls and three internal obstacles form a
    # horizontally alternating course. The final internal pair provides a
    # 1.9 m raw / 1.2 m effectively inflated challenge corridor. Moving that
    # pair 0.7 m right leaves a 1.0 m planning-safe turning gap after O4.
    obstacles:
      [
        2.6, -0.5, 2.35, 0.8, 4.0, 4.7,
        4.6, 4.15, 2.35, 0.8, 4.7, 4.7,
        6.7, 0.8, 2.35, 0.8, 3.2, 4.7,
        8.9, 3.75, 2.35, 0.8, 5.5, 4.7,
        11.4, -0.85, 2.35, 0.8, 1.3, 4.7,
        11.4, 2.95, 2.35, 0.8, 2.5, 4.7
      ]

    safety_radius: 0.25
    odometry_timeout: 0.25

    # Default planning contract (not planner parameters):
    # start = (0.0, 0.0, 1.5), goal = (13.2, 5.5, 1.5)
    # grid resolution = 0.25 m, effective planning radius = 0.35 m
