/**:
  ros__parameters:
    frame_id: map

    # [xmin, xmax, ymin, ymax, zmin, zmax]
    workspace: [-1.0, 10.0, -3.0, 3.0, -0.5, 5.0]

    # Two staggered wall pairs force an S-shaped route.  Each wall has a
    # 1.8 m raw opening; with the unchanged 0.25 m safety radius and 0.10 m
    # planning margin, the effective planning width is 1.1 m.
    # Each obstacle is [center_x, center_y, center_z, size_x, size_y, size_z].
    obstacles:
      [
        3.0, -1.35, 2.35, 0.8, 3.3, 4.7,
        3.0,  2.55, 2.35, 0.8, 0.9, 4.7,
        6.0, -2.45, 2.35, 0.8, 1.1, 4.7,
        6.0,  1.45, 2.35, 0.8, 3.1, 4.7,
        8.2,  2.35, 2.35, 1.0, 1.0, 4.7
      ]

    safety_radius: 0.25
    odometry_timeout: 0.25

    # Assessment contract:
    # start = (0.0, 0.0, 1.5), goal = (8.5, 0.0, 1.5)
    # resolution = 0.25 m, planning_margin = 0.10 m
